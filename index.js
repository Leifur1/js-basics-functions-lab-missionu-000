// Code your solution in this file!

function distanceFromHqInBlocks (distance) {
  if (distance < 42) {
    let distance = 42 - distance;
  } else {
    let distance = distance % 42
  }
  return dist
}
